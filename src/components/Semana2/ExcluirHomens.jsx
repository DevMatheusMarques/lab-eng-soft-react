import React from "react";

const ExcluirHomens = ({ onClick }) => {
    return (
        <button className="mt-5 me-3 btn border border-info-subtle bg-info-subtle" onClick={onClick}>-</button>
    );
};

export default ExcluirHomens;
